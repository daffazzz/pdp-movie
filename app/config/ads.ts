/**
 * Global Ads Configuration
 *
 * Set ADS_ENABLED to `true` to re-enable all advertisements across the site.
 * When `false`, every ad component will render nothing (return null).
 */
export const ADS_ENABLED = false;
